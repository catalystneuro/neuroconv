Added general-anatomy annotation to `neuroconv.tools.ontology`: `infer_anatomy_ontology_metadata` resolves `ndx-pose` `Skeleton.nodes` entries to a UBERON term for common skeleton parts and muscles, plus every structure of the optional `neuro-termsets` package when installed, and writes it to `metadata["PoseEstimation"]["ontology"]["anatomy"]`, and `add_anatomy_external_resources` writes those stated terms into the file as HERD references.