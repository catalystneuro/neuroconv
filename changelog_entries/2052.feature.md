Added `IntanAnalogEventsInterface` for deriving discrete events from ADC inputs and outputs, and a shared `detection_configuration` on `IntanConverter` that selects digital lines and ADC channels to convert as events. Digital lines keep their default conversion; ADC event detection is opt-in.
