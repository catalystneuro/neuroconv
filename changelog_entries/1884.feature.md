Added strain annotation to `neuroconv.tools.ontology`: `infer_strain_ontology_metadata` resolves `Subject.strain` to an RRID term for common laboratory strains and writes it to `metadata["Subject"]["ontology"]["strain"]`, and `add_strain_external_resource` writes that stated term into the file as a HERD reference.