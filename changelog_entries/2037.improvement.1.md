`ExternalVideoInterface` now checks the times against each file's frame count when writing, instead of only their total against all the files' frames, so times set on the wrong file raise with that file's name.
