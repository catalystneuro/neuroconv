Bumped the minimum version of `pynwb` to `>=4.2.0`, the first release that writes `Units.waveform_unit`, whose value earlier releases discarded, and that carries the new `Units.waveform_time_before_peak_in_ms`.
