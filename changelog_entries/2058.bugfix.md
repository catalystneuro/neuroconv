A GuPPy NPM session now converts from the record GuPPy writes of what it demultiplexed — the source file, excitation wavelength and column behind each store — rather than by reproducing that demultiplexing from the store's name. Run folders written before GuPPy recorded it keep converting through the older name-decoding path.
