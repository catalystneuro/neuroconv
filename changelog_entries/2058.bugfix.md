A GuPPy NPM session now converts under the store names GuPPy gives it, which name the source file, the excitation wavelength and the region column a channel was demultiplexed from rather than a file index and two positional ordinals, so reading one back no longer means reproducing GuPPy's demultiplexing arithmetic.
