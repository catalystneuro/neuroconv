Changelog entries are now written one file per entry under `changelog_entries/`, named `<PR number>.<type>.md`, and assembled into `CHANGELOG.md` by `towncrier build` at release time, so two pull requests no longer edit the same lines.
