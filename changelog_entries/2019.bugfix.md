`BaseImagingExtractorInterface.get_metadata` now files its `MicroscopySeries` entry under `default_metadata_key` when the interface was constructed without a `metadata_key`, which is the key `add_imaging_to_nwbfile` falls back to. The entry was keyed by `None`, so a direct subclass that left the key alone raised `KeyError` on its first write.
