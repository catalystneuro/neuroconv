`BaseImagingExtractorInterface` now resolves an omitted `metadata_key` to `default_metadata_key` in `__init__`, the key `add_imaging_to_nwbfile` falls back to. `get_metadata` had filed its `MicroscopySeries` entry under `None`, so a direct subclass that left the key alone raised `KeyError` on its first write.
