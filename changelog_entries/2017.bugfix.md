`AudioInterface.set_aligned_starting_time` now shifts the starting time of a single audio file that already has one, so a second call, or a call after `set_aligned_segment_starting_times`, accumulates as it does on the other interfaces instead of raising. `add_acoustic_waveform_series` also returns the `NWBFile` on its normal path, as its signature states, rather than only when the series was already present.
