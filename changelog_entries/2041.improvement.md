Every `ImageSeries.external_file` entry is now written relative to the NWB file, with forward slashes, whether the interface was given an absolute path or one relative to the working directory, so an external video no longer fails NWB Inspector's `check_image_series_external_file_relative` and still resolves once the file and its videos move together.
