`dict_deep_update` now forwards `compare_key` and `list_dict_deep_update` into the nested mappings and list entries it recurses into, where both had fallen back to their defaults, so a `compare_key` other than `name` no longer raises `KeyError` below the top level and `list_dict_deep_update=False` replaces nested list entries as asked.
