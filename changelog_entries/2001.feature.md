Recording interfaces now accept electrode rows and column specifications through `metadata["Ecephys"]["ElectrodesTable"]`, with `channel_to_electrode` linking channels to rows, and support ID-based probe wiring through `set_probe(channel_id_to_contact_id=...)`.
