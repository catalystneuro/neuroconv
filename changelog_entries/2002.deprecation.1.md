Naming `shuffle` in a `ZarrDatasetIOConfiguration`'s `filters` is deprecated, to be removed in v0.12.0, and `shuffle` no longer appears in `AVAILABLE_ZARR_COMPRESSION_METHODS`, since it rearranges the serialized bytes rather than the values and so belongs in `compressors` beside the compression method.
