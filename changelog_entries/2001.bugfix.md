Explicit electrode annotations now retain their channel associations when a probe is attached, inheriting contact identity and geometry through the saved `channel_to_electrode` mapping and rejecting missing mappings or conflicting contact identities.
