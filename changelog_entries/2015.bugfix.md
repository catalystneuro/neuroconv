`add_segmentation_to_nwbfile` called without metadata no longer refuses an extractor that holds ROIs but no traces. The placeholder metadata it fills in carries a `RoiResponses` entry, which the trace writer took for metadata the caller had written and rejected for lacking the traces to match, so a masks-only segmentation converted only when the caller wrote out metadata that named no traces.
