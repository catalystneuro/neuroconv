Recording interfaces now default to SpikeInterface's automatic probe/shank/side grouping, with explicit `by_probe`, `by_shank`, and `by_side` overrides and `group_property` for further subdivision by a per-contact annotation.
