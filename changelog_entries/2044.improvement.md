The series inside a `PoseEstimation` container now link to a single timestamps dataset instead of each writing its own copy, which takes a ten-keypoint container over five thousand irregularly sampled frames from 1.02 MB to 0.65 MB and leaves regularly sampled data, written as a rate, untouched.
