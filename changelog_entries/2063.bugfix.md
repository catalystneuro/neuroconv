Added a `zarr<3` pin, since hdmf-zarr 0.14.0 requires zarr 3 and neuroconv's Zarr backend uses the zarr 2 API, so a fresh `pip install neuroconv` failed at import with `AttributeError: module 'zarr' has no attribute 'codec_registry'`.
