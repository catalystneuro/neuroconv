`PhySortingInterface` now adds a `max_channel` column to the units table, giving each unit the channel on which its template has the largest peak-to-peak amplitude, the same rule Phy uses for a unit's best channel. It references the electrodes table when the two describe the same channels, and can be turned off with `include_max_channel=False`.
