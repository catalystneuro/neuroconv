`ZarrDatasetIOConfiguration.filter_methods` is renamed to `filters`, with the old name deprecated for removal in v0.12.0, so a dataset's codec stages are named the way `zarr.Array` names them: `filters` for the codecs applied to the values and `compressors` for those applied to the bytes they serialize to.
