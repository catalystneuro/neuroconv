`test_get_json_schema_from_method_signature_docstring_warning` now counts only the typo warning it tests for, since `pytest.warns` also records unrelated warnings left over from earlier tests on the same worker, which made it fail intermittently on Python 3.10.
