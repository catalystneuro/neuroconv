The events interfaces now list their event types with `get_event_type_source_ids` from their header or configuration and build the `event_types` metadata from that list instead of the other way round, so listing the types reads no events for TDT and per-array MedPC files, whose configuration errors now surface on the first read instead of in `get_metadata`.
