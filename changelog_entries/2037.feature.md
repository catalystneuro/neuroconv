`ExternalVideoInterface` now holds the `alignment` surface, one object per video file keyed by the file stem, and the surface gained `alignment[key].start_at(starting_time)`, which places one object by its first sample without building its times. The `set_aligned_*` methods and `get_timestamps` still work under a `FutureWarning` and will be removed in v0.12.0.
